const prompt = require('prompt-sync')();
// 02) Classifique o triangulo: Equilatero (3 lados iguais), Isosceles (2 iguais) ou Escaleno (todos diferentes).
function classificaTriangulo(l1, l2, l3) {
    // TODO: Implementar
}
