const prompt = require('prompt-sync')();
// 38) Imprima impares entre inicio e fim. Se inicio maior que fim, troque-os. Default: 0 a 100.
function imprimirImpares(inicio = 0, fim = 100) {
    // TODO
}
