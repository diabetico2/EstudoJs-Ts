const prompt = require('prompt-sync')();
// 36) Funcao 1: Multiplica elemento por numero. Funcao 2: Multiplica so se valor for maior que 5.
function multiplicaVetor(vetor, num) {}
function multiplicaSeMaiorQue5(vetor, num) {}
