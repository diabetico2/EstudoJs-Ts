const pontuacaoUsu = 999;
const nivelUsuario = pontuacaoUsu >= 1000 ? 'usuario vip' : 'Usuario comum' //*para true (?) para false (:)
console.log(nivelUsuario)

const corUsu = null;
const corPadrao = corUsu || 'vermelho';
// if (pontuacaoUsu >= 1000) {
//   console.log("vip");
// } else {
//   console.log("normal");
// }
